import { describe, it } from 'mocha';
import { expect, assert } from 'chai';

describe('grammar', () => {
  it('Should parse some questions', function test(done) {
      assert.isOk(true, 'everything is ok');
      assert.isOk(false, 'thingsare broken');
      done();
  });
});
